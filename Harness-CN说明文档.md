# Harness-CN 说明文档

**版本**：0.1.5-rc.1 ｜ **平台**：Windows 10 / 11 x64 ｜ **许可证**：MIT（基于上游修改）

---

## 一、这是什么

Harness-CN 是 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（下称 DSH）的**第三方 Windows 桌面发行版**。DSH 本身是一个"一切皆插件"的 coding agent 运行时——会话、工具、子代理、工作流、沙箱与审批、Web 界面，全部由 Cordis 插件组合而成；官方以 npm 包和 `dsh` 命令行的形式发布，使用前需要自己准备 Node 环境、拉取 profile、安装插件。

Harness-CN 只做一件事：把这一整套东西**打成一个能双击安装的 Windows 程序**，并解决它在真实桌面上会遇到的启动、升级与兼容问题。安装后不需要 Node、pnpm、Python、Git 或任何编译工具链，也不需要联网安装依赖。

它与官方的关系是"再发行"而不是"分支开发"：内核代码、插件体系、Web 界面全部来自上游，本发行版改的是**打包、启动、桌面外壳与客户端健壮性**。因此上游文档里的概念（插件、Slot、Agent Loop、Session 等）在这里完全适用。

## 二、安装

1. 在本仓库的 **Releases** 页面下载 `Harness-CN-0.1.5-rc.1-win-x64-setup.exe`。
2. 双击安装。安装包**没有代码签名**，SmartScreen 会提示"未知发布者"，点「更多信息 → 仍要运行」；部分企业策略会直接拦截未签名程序，这种情况需要联系管理员。
3. 安装向导可以选择安装目录、**按用户安装**（不需要管理员权限）。默认不会注册系统服务、不写开机自启、不改系统 PATH。
4. 卸载：从"设置 → 应用"卸载或运行安装目录里的卸载程序。你的个人数据在 `~/.dsh`（即 `C:\Users\<你>\.dsh`）下，卸载不会删除，需要时自行清理。

**环境要求**：Windows 10 1809 以上或 Windows 11，x64；用户目录所在盘预留约 1 GB；能访问 `api.deepseek.com`。ARM64 的 Windows 只能以 x64 模拟方式运行。macOS / Linux 请使用上游发行版。

## 三、首次启动会经历什么

安装包里的 `resources/seed` 不是散装的 npm 包，而是一份"已经安装好的运行时"的归档：pnpm store 的 16 个分片、一份"哪个文件由 store 里哪个文件提供"的链接清单，以及少量必须随包携带的文件。启动时把它"链接"回本地，而不是重新安装一遍。

| 阶段 | 首次启动 | 之后每次启动 |
| --- | --- | --- |
| 校验种子（逐文件 SHA-256） | 约 3 秒 | 同版本直接跳过 |
| 解包并合并 pnpm store | 约 30 秒 | 已含该 seed 时整段跳过 |
| 重建运行时（约 2.7 万个硬链接） | 约 15 秒 | 已安装且同一 seed 时跳过 |
| 暂存运行时健康检查（真起一次后端） | 10–60 秒 | 跳过 |
| 激活 + 正式启动后端 | 约 7 秒 | 约 7 秒 |

启动窗口会在 1–2 秒内出现，带进度条和循环播放的加载动画，所以等待期间不会是"空屏卡死"。每一步的日志写在：

```
%APPDATA%\@deepseek-ai\dsh-desktop\logs\harness.log
```

遇到问题把这个文件的尾部几行发到 Issues，是最快的定位方式。

## 四、绑定 API Key

第一次启动会在启动界面要求绑定 DeepSeek API Key。三种方式任选：

1. 在启动界面填写并确认；
2. 进入主界面后，顶部菜单 →「重新绑定 API-Key」；
3. 直接编辑 `~/.dsh/.credentials.yaml`（格式为 `version / refs / records` 三段，`refs.DEEPSEEK_API_KEY` 放你的 key），保存后重启应用。

Key 只保存在本机，**不会随安装包分发**，也不会出现在会话日志里。如果你在多台机器上用，每台各绑一次即可。

## 五、界面导览

- **顶部菜单**：重启 / 运行日志 / 检查更新 / 重新绑定 API-Key / 开发者工具 / 退出。本发行版未签名、没有更新源，所以「检查更新」只会报告空闲，升级请手动安装新包。
- **左侧**：工作区（按文件夹分组）与会话列表。支持搜索、分组方式与排序切换、拖动排序、折叠展开；会话按工作区分组，未归组的会话单独一栏。
- **中间**：对话区。助手回复、思考过程、工具调用与结果按时间线折叠展示；顶部有会话标题、模型选择、上下文占用与费用等信息。
- **设置页**（左侧分栏）：
  - **通用设置**：外观、语言、快捷键、启动行为等；
  - **模型**：默认模型、各模型的推理强度、自定义 provider 与 API 地址；
  - **插件**：查看已装插件、编辑插件配置、增删插件；
  - **Agent 预设**：为不同任务切换 agent 组合（例如只读咨询、全工具编码）；
  - **会话导入**：从 Claude / Codex / Grok 等外部工具导入历史会话，并支持双向增量同步；
  - **费用**：本会话、当日费用与历史记录，支持多厂商价格目录与余额查询。
- **辅助窗口**：启动窗口、API-Key 绑定窗口、运行日志窗口都是独立窗口，**不显示菜单栏**，避免工作窗口与辅助窗口菜单混淆。

## 六、常用操作

- **新建会话**：左侧顶部的加号；会话与工作区绑定，切换工作区即切换上下文目录。
- **切换模型**：对话区顶部或设置 → 模型。默认模型、子代理可用模型都可以单独配置。
- **附加文件 / 图片**：直接拖入输入框，或粘贴截图；图片会走多模态视觉通路。
- **引用文件**：在输入框里输入 `@` 触发文件补全，选中的路径会作为工作区引用注入。
- **权限与审批**：设置 → 通用设置里的权限预设决定工具是否需要逐次确认。预设从"只读"到"完全放行"分档，"完全放行"意味着 agent 可以不经确认地读写文件、执行命令——在不熟悉的项目里建议先用更保守的档位。
- **计划模式 / 目标 / 待办**：计划模式让 agent 先给出方案再动手；Goal 用于跨多轮的长任务；待办清单用于把一次任务拆成可勾选的步骤。
- **子代理与工作流**：子代理把独立子任务放到独立上下文里执行（可指定不同模型），工作流用于大规模扇出（多文件审计、批量迁移等）。
- **任务与终端**：长时间运行的命令可以放到后台任务里，随时查看输出；内置终端与持久 shell 会话。
- **技能（Skills）**：把可复用的操作规范写成技能文件，agent 会在匹配时自动加载。
- **其他**：MCP 插件接入、Web 搜索与抓取、会话导出、压缩（长会话自动/手动摘要）、定时任务、Webhook 接入等，都能在设置或命令面板里找到。

## 七、随包预置的第三方插件

| 插件 | 作用 |
| --- | --- |
| `dsh-chat-import` | 会话导入与双向同步（Claude / Codex / Grok 等），设置页的「会话导入」分区由它提供 |
| `dsh-cost-meter` | 会话费用、当日费用、历史记录与余额查询，多厂商价格目录 |
| `dsh-rule-engine` | 规则引擎，按规则对会话与工具调用做提示或拦截 |
| `@liustack/modlens` | 视觉引擎（ModLens），让纯文本模型也能"看懂"截图与设计稿 |
| `@jieai/dsh-plugin-vet` | 插件审计（安装前静态扫描与健康档案） |
| `@anionex/dsh-vision-toolkit` | 视觉工具包，**默认不激活**——它首次运行会下载约 35 MB 的独立 Python 运行时，会拖住启动；需要时在设置 → 插件里启用 |

这些插件随安装包预置，首次启动即可用，不需要联网安装。

## 八、数据与备份

| 路径 | 内容 | 可否删除 |
| --- | --- | --- |
| `~/.dsh/profiles/desktop` | 已安装的运行时（约 270 MB） | 可删，下次启动自动重建 |
| `~/.dsh/desktop/pnpm/store` | 本地 pnpm store 与暂存目录（约 260 MB） | 可删，代价是重建时多花约 30 秒解包 |
| `~/.dsh/settings.yaml` | 你的设置：默认模型、权限预设、主题、语言等 | 删掉即恢复默认，需重新配置 |
| `~/.dsh/.credentials.yaml` | API Key 等凭据 | 删掉需重新绑定 |
| `~/.dsh/storages/` | 插件自己的数据（如费用账本、工作区记录） | 删掉会丢历史统计 |
| `%APPDATA%\@deepseek-ai\dsh-desktop\logs\harness.log` | 桌面外壳与后端日志 | 可删 |
| `%APPDATA%\@deepseek-ai\dsh-desktop\`（其他子目录） | 窗口状态、本地存储等界面状态 | 可删，界面偏好会重置 |

**迁移到新机器**：把整个 `~/.dsh` 目录复制过去即可（包含设置、凭据、插件数据与已装运行时）；如果新机器上运行时目录不完整，启动时会自动重建。

## 九、故障排查

**启动时弹出「Harness-CN 无法启动」**：对话框第一行就是根因。按文字对号入座：

- `plugin tree failed to load ... Invalid or unexpected token` 或 `Cannot find module ...spill.js`：运行时的文件内容错位。早期版本用文件 inode 配对 store 条目，在 Windows 上会因精度丢失而配错；0.1.5-rc.1 起改为按内容摘要配对。请安装最新安装包。
- `desktop project: core package mapping does not match desktop-packages.json`：profile 里的 `pnpm-workspace.yaml` 被 pnpm 追加了自己的段落。新版本只校验核心映射是否仍然存在。
- `ENOENT: ... profiles\desktop\package.json`：profile 是"删除没删干净"的残骸（只剩 `node_modules`）。新版本会把它当作全新安装处理。
- 其他文字：把对话框全文与 `harness.log` 尾部一起发到 Issues。

**启动很慢或像卡住**：先看启动窗口的进度条是否在动，再看日志最后一行停在哪一阶段。首次安装后的第一次启动会做一次完整的解包与链接，属于预期。

**面板报错**（例如「会话导入」提示"服务响应异常"）：这是插件 POST 路由没有到达后端。0.1.5-rc.1 起桌面载体已把所有方法都转交内部 webserver，安装最新包即可。

**侧边栏工作区列表崩溃**：旧版本持久化的界面存档缺少新字段导致。新版本复水时会把存档合并到默认值之上，无需手动清缓存。

**想彻底重置**：退出应用 → 删除 `~/.dsh/profiles/desktop` 与 `~/.dsh/desktop` → 重新启动（设置与凭据不受影响）。

**模型调用失败**：确认 API Key 有效、网络能访问 `api.deepseek.com`、设置 → 模型里选中的模型在你的账号可用。

## 十、从源码构建

前置：Windows x64、Node 22.19+ 或 24+、git、pnpm 11.7.0；只有需要从源码编译原生模块时才需要 Python（`node-pty`、`koffi` 有预编译产物）。

```powershell
powershell -File build-harness-cn.ps1              # 含 pnpm install
powershell -File build-harness-cn.ps1 -SkipInstall
```

脚本会设置本发行版的应用标识与"未签名"模式、切换 Electron 镜像源，然后执行上游的 `package:desktop:win:x64`（构建 → 打包 tarball → 准备运行时 → 准备种子 → electron-builder）。产物在：

```
apps/desktop/.desktop-build/targets/win-x64/artifacts/
  ├── harness-cn-0.1.5-rc.1-win-x64.exe      # 安装包
  └── win-unpacked/                          # 免安装目录（含 resources/seed 与 runtime）
```

安装包超过 GitHub 单文件 100 MB 上限，必须作为 **Release 附件**发布，不能直接提交进仓库。

## 十一、已知限制

- 仅 Windows x64；未签名；无自动更新（升级=手动安装新包）。
- `~/.dsh` 与 `dsh` 命令行共用同一个 home：同一台机器上混用其他 DSH 构建时会互相覆盖 profile 与 store，建议只保留一个正在使用的构建。
- 首次启动需要约 500 MB 磁盘（store 260 MB + 运行时 270 MB）。
- 预置的视觉工具包默认关闭（原因见第七节）。

## 十二、许可证与致谢

本发行版基于 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)（MIT）修改，上游版权归 DeepSeek 所有；本仓库的改动同样以 MIT 发布，详见 `LICENSE`。第三方组件与许可证清单见 `THIRD_PARTY_NOTICES.md`，`vendor/` 是上游 vendored 源码副本（来源记录在 `vendor/README.md`）。本仓库**不是** DeepSeek 官方发行版，问题请提到本仓库而不是上游。
